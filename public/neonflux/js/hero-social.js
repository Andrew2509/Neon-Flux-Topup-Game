/**
 * Hero Social Buttons Logic
 * Placeholder for future interactions or analytics tracking
 */
document.addEventListener('DOMContentLoaded', function() {
    const socialButtons = document.querySelectorAll('.hero-social-Btn');

    socialButtons.forEach(btn => {
        btn.addEventListener('mouseenter', () => {
            // Optional: Track hover analytics
        });
    });
});
